// $('.zone').empty();
$('#loading-server').remove();
english = '<div style = "color: white; width:600px; margin:0 auto" align="center"><h2>Hall of Fame</h2><label>Your Twitter Followers</label><br>' +
    '<table class="table"><thead align="center"><tr><td>Rank</td><td>name</td><td>Score</td><td>Trophies' +
         '</td></tr></thead>' +
    '<tbody align="center">' +
    '<% common = Authentication.get_common_twitter_followers(current_gamer) %>'+
    '<% if common.nil? %>' +
    '<p>You are not connected to Twitter</p>' +
    '<% else %>' +
    '<% @gamers = Gamer.find(common, :order => "highest_score DESC") %>' + 
    '<% rank = 0 %>'+
    '<% @gamers.each do |user| %>'+
    '<tr>' +
      '<td><%= rank = rank + 1 %></td>' +
      '<td><%= user.username %></td>' +
      '<td><%= user.highest_score %></td>' +
      '<% @Trophies = Gamers_Trophy.find_by_gamer_id(user.id) %>' +
      '<td>' +
        '<% if @Trophies.nil? %>' +
        '<p>No Trophies</p>' +
        '<% else %>' +
        '<%= @Trophies.trophy_id %>' +
        '<% end %>' +
      '</td>' +
    '</tr>' +
    '<br>' +
    '<% end %>' +
    '<% end %>' +
  '</tbody>' +
'</table></div>';
arabic = '<div style = "color: white; width:600px; margin:0 auto" align="center"><h2>قاعة المجد</h2><label>أصدقائك في تويتر</label><br>' +
    '<table class="table"><thead align="center"><tr><td>ترتيب</td><td>الإسم</td><td>مجموع النقاط</td><td>الجوائز' +
         '</td></tr></thead>' +
    '<tbody align="center">' +
    '<% common = Authentication.get_common_twitter_followers(current_gamer) %>'+
    '<% if common.nil? %>' +
    '<p>You are not connected to Twitter</p>' +
    '<% else %>' +
    '<% @gamers = Gamer.find(common, :order => "highest_score DESC") %>' + 
    '<% rank = 0 %>'+
    '<% @gamers.each do |user| %>'+
    '<tr>' +
      '<td><%= rank = rank + 1 %></td>' +
      '<td><%= user.username %></td>' +
      '<td><%= user.highest_score %></td>' +
      '<% @Trophies = Gamers_Trophy.find_by_gamer_id(user.id) %>' +
      '<td>' +
        '<% if @Trophies.nil? %>' +
        '<p>لا يوجد جوائز</p>' +
        '<% else %>' +
        '<%= @Trophies.trophy_id %>' +
        '<% end %>' +
      '</td>' +
    '</tr>' +
    '<br>' +
    '<% end %>' +
    '<% end %>' +
  '</tbody>' +
'</table></div>';
<% if I18n.locale==:en %>
$(".zone").append($(english));
<% else I18n.locale==:ar %>
$(".zone").append($(arabic));
<% end %>
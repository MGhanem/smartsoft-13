<% if I18n.locale==:en %>
<h2>Register as developer Step2: choose your subscription model</h2>
<% else I18n.locale==:ar %>
<h2>تسجيل الدخول المطور: المرحلة رقم 2 ، إختر نموذج الاشتراك</h2>
<% end %>

<%= form_for(:my_subscription, :url => { :controller => "my_subscription",:action => "pick" }) do |f| %>
<div style = "width:1000px; margin: 0 auto;">
<table>
		<tr>
<% @all_subscription_models.each do |sm| %>
	<%#= radio_button_tag "subs", sm.id, %>
	<%if(@developer.my_subscription)
		selected_sub_id = @developer.my_subscription.subscription_models_id
	else
		selected_sub_id = 1
	end%>
	
			<td width="300px">
	<div class="well" style="margin-left: 30px; box-shadow: 0 4px 2px #ccc;">
	<p><%= radio_button_tag(:my_subscription, sm.id, sm.id == selected_sub_id) %> <%= sm.name %><br /><h5>The limit for the word search is:</h5>
		<%=sm.limit_search%><br />
		<h5>The limit for the word follow is:</h5>
		<%=sm.limit_follow%><br/>
		<h5>The limit for the word add is:</h5>
		<%=sm.limit%><br />
		<h5>The limit for the projects:</h5>




	</p>
    </div>
            </td>
        
<% end %>
<tr>
    </table>

<center>
<% if I18n.locale==:en %>
<%= submit_tag "Select Subscription", :class => "btn btn-success btn-large", :style => "margin-top: 20px; 	" %>
<% else I18n.locale==:ar %>
<%= submit_tag "إختر"%>
</div>
</center>
<% end %>
<% end %>
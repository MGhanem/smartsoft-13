<!DOCTYPE html>
<html>
<head>

<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title><%= content_for?(:title) ? yield(:title) : "Arability" %></title>
<meta name="author" content="Karim ElNaggar">
<%= stylesheet_link_tag "admin", :media => "all" %>
<%= csrf_meta_tags %>
<%= yield(:head) %>

</head>
<body>

<div role="main" id="main">
  <div class="container">
    <% flash.each do |key,msg| %>
      <div class="alert alert-<%= key %>">
        <div class="message-inner">
          <%= content_tag :div, msg %>
        </div>
      </div>
    <% end %>
    </div>
  </div>
</div>

<%= yield %>

<%= javascript_include_tag "admin" %>

</body>
</html>
